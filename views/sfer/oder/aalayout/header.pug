.navbar.navbar-expand-lg.navbar-dark.bg-dark.fixed-top
	a.navbar-brand(href="/") orderPart 
	button.navbar-toggler(type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation")
		<span class="navbar-toggler-icon"></span>

	.collapse.navbar-collapse#navbarTogglerDemo02
		ul.navbar-nav.mr-auto.mt-2.mt-lg-0
			li.nav-item.active
				a.nav-link(href="/odOrders") Order <span class="sr-only">(current)</span>
			li.nav-item.active
				a.nav-link(href="/odPays") Pay <span class="sr-only">(current)</span>
			li.nav-item.active
				a.nav-link(href="/odVders") Vder <span class="sr-only">(current)</span>
		ul.navbar-nav
			li.text-info
				span &nbsp;
				a.btn.btn-info(href="/oderInfo") hello: 
					if crSfer.name && crSfer.name.length > 0
						span #{crSfer.name}
					else
						span #{crSfer.code}
					if crSfer.role
						span &nbsp;(#{Conf.sfRole[crSfer.role]})
					if crSfer.photo
						img(src=Svaddr+crSfer.photo, width="30")
					-a;
			span &nbsp;&nbsp;&nbsp;&nbsp;
			li: a.btn.btn-danger(href="/logout") Logout

.container
	.row
		div(style="height:80px") 


#signIn.modal.fade
	.modal-dialog
		.modal-content
			form(method="post", action="/loginMger")
				.modal-header: h3 Manager Signin
				.modal-body
					.form-group
						label.col-form-label(for="iptCode") Code
						input#iptCodeModal.form-control(type="text", class="iptCode", name="code")
					.form-group
						label.col-form-label(for="iptPassword") Password
						input#iptPasswordModal.form-control(type="password", class="iptPassword", name="password")
				.modal-footer
					button.btn.btn-default(type="button" data-dismiss="modal") 关闭
					button.btn.btn-success(type="submit") 提交