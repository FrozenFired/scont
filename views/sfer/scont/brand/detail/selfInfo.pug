-let divBg = "bg-light";

if object.status == 0
	-divBg ="bg-info";
else if object.status == 1
	-divBg ="bg-white";
else if object.status == 2
	-divBg ="bg-success";
else if object.status == 3
	-divBg ="bg-warning";
else if object.status == 4
	-divBg ="bg-secondary";
else if object.status == 5
	-divBg ="bg-danger";
.row(class=divBg)
	h1.col-sm-6
		span brand Information: [#{object.code}]
	.col-sm-6.text-right
		a.btn.btn-link(href="/brandList") brand list

hr
.bg-light
	table.table.table-hover.table-bordered
		tr
			th brand code
			th nation
			th firm name
			th category1
			th category2
			th material Description

		tr
			td.text-primary <strong>#{object.code}</strong>
			td
				if object.nation
					-a;
					span #{object.nation.code}<br/>
			td #{object.firmName}
			if object.bcateg
				-a;
				td #{Conf.bcate[object.bcateg.bcate]}
				td #{object.bcateg.code}
			else
				td
				td
			td #{object.matDesp}
	table.table.table-hover.table-bordered
		tr
			th website
			th iva
			th product Time
			th price list
			th atlas
		tr
			td 
				span #{object.website}
				if object.webNote
					-a;
					span <br/>#{object.webNote}
			td #{object.iva}
			td #{object.pTime}
			td 
				span #{object.plist} 
				if object.plNote && object.plNote.length > 0
					-a;
					span : #{object.plNote}
			td 
				span #{object.atlas} 
				if object.atNote && object.atNote.length > 0
					-a;
					span : #{object.atNote}
	table.table.table-hover.table-bordered
		tr
			th status
			th staff
			th createAt<br/>updateAt
			th weight
			th option
		tr
			td(class=divBg) #{Conf.stsBrand[object.status]}
			td
				if object.creater
					-a;
					span creater: #{object.creater.code}
				if object.updater
					-a;
					br
					span updater: #{object.updater.code}
			td 
				span #{moment(object.createAt).format('MM/DD/YYYY HH:mm')}
				br
				span #{moment(object.updateAt).format('MM/DD/YYYY HH:mm')}
				
			td #{object.weight}
			td
				a.btn.btn-dark.btn-lg(href="/brandUpdate/"+object._id) update
				button.btn.btn-warning.btn-lg#mark(type="button") Mark
script(src="/js/scont/markToggle.js")