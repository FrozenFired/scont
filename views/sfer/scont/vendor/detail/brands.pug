if object.sconts && object.sconts.length > 0
	-let sconts = object.sconts;
	hr
	h2 #{sconts.length} - Brands:
	table.table.table-hover.table-bordered
		thead
			tr
				th Brand
				th Category
				th scont
				th Note
				th status
				th view
		tbody
			each scont in sconts
				- let trBg;
				if scont.status == 0
					-trBg ="bg-info";
				else if scont.status == 1
					-trBg ="bg-default";
				else if scont.status == 2
					-trBg ="bg-success";
				else if scont.status == 3
					-trBg ="bg-warning";
				else if scont.status == 4
					-trBg ="bg-secondary";
				else if scont.status == 5
					-trBg ="bg-danger";
				tr(class=trBg+" scont-id-"+scont._id)
					if scont.brand
						-brand=scont.brand;
						td: a.btn.btn-primary(href="/brandDetail/"+brand._id) #{brand.code}
						if brand.bcateg
							-bcateg=brand.bcateg;
						td #{Conf.bcate[bcateg.bcate]}
					else
						td
						td
					td #{scont.scont}%
					td #{scont.note}
					td #{Conf.stsScont[scont.status]}
					td: a.btn.btn-primary(href="/scontDetail/"+scont._id) view