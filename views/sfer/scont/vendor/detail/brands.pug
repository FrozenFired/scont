if object.sconts && object.sconts.length > 0
	-let sconts = object.sconts;
	hr
	h2 #{sconts.length} - Brands:
	table.table.table-hover.table-bordered
		thead
			tr
				th Brand
				th Category
				th scont
				th Note
				th status
				th view
		tbody
			each scont in sconts
				- let scontBg;
				if scont.status == 0
					-scontBg ="bg-info";
				else if scont.status == 1
					-scontBg ="bg-default";
				else if scont.status == 2
					-scontBg ="bg-success";
				else if scont.status == 3
					-scontBg ="bg-warning";
				else if scont.status == 4
					-scontBg ="bg-secondary";
				else if scont.status == 5
					-scontBg ="bg-danger";
				tr(class=scontBg+" scont-id-"+scont._id)
					if scont.brand
						-brand=scont.brand;
						if brand.status == 4
							-brandBg ="bg-secondary";
						else
							-brandBg ="bg-default";

						td(class=brandBg): a.btn.btn-primary(href="/brandDetail/"+brand._id) #{brand.code}
						if brand.bcateg
							-bcateg=brand.bcateg;
						td(class=brandBg) #{Conf.bcate[bcateg.bcate]}
					else
						td
						td
					td #{scont.scont}%
					td #{scont.note}
					td #{Conf.stsScont[scont.status]}
					td: a.btn.btn-primary(href="/scontDetail/"+scont._id) view