extends ../../aalayout/layout

block content
	.container
		h1 nation Information:
		hr
		include ./detail/selfInfo
		if crSfer.role == 5 || crSfer.role == 10
			-a;
			if brands.length > 0 
				-a;
				hr
				include ./detail/brands