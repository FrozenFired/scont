extends ../aalayout/layout

block content
	.container
		.row(class=objBg)
			h1.col-sm-9 Brand Category: [#{object.code}]
			.col-sm-3.text-right
				a.btn.btn-link(href="/bnBcategs") category list
		hr
		include ./detail/selfInfo
		if brands && brands.length > 0
			-a;
			hr
			include ./detail/brands
		.row
			.col-sm-8
			.col-sm-4
				a.btn.btn-info.btn-lg.btn-block(href="/bnBcategPrint/"+object._id) DownLoad Excel