extends ../aalayout/layout

block content
	.container
		.row
			h1.col-12.col-sm-6 nation: [#{object.code}]
			.col-12.col-sm-6.text-right
				a.btn.btn-link(href="/bnNations") nation list
		hr
		include ./detail/selfInfo
		if brands.length > 0 
			-a;
			hr
			include ./detail/brands