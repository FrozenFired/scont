extends ../aalayout/layout

block content
	.container
		.row(class=objBg)
			h1.col-sm-9 nation Information: [#{object.code}]
			.col-sm-3.text-right
				a.btn.btn-link(href="/bnNations") nation list
		hr
		include ./detail/selfInfo
		if brands.length > 0 
			-a;
			hr
			include ./detail/brands