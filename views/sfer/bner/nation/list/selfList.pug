-var i = 0;
each item in objects
	- i++;
	.row.nationlist.mt-4(class="item-id-"+item._id)
		.col-md-10
			a.text-dark(href="/bnNation/"+item._id)
				.card
					.row.m-1
						.col-12.col-sm-6
							.row
								.col-4: strong #{i}
								h4.col-4 #{item.code}
								.col-4 #{item.tel}
						.col-12.col-sm-6
							.row
								.col-4 #{item.nameCN}
								.col-4 #{item.nameEN}
								.col-4.text-info #{item.numbrand}
		.col-md-2
			if !item.numbrand
				-a;
				button.btn.btn-danger.btn-block.del.mt-1(type="button", data-urldel="/bnNationDel", data-id=item._id) 删除
script(src="/js/delete.js")