.navbar.navbar-expand-lg.navbar-dark.bg-dark.fixed-top
	a.navbar-brand(href="/vder"): img(src="https://static.casacdn.com/static/images/host/logo_new.png", width="40")
	button.navbar-toggler(type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation")
		<span class="navbar-toggler-icon"></span>

	.collapse.navbar-collapse#navbarTogglerDemo02
		ul.navbar-nav.mr-auto.mt-2.mt-lg-0
			if crVder
				-a;
				li.nav-item.active
					a.nav-link(href="/vdBrandList") brands <span class="sr-only">(current)</span>
		ul.navbar-nav
			if crVder
				-a;
				//- li
				//- 	form.form-inline(method="get", action="/headerBrand", enctype="multipart/form-data")
				//- 		input(type="hidden", name="keytpye", value="code")
				//- 		input.form-control.mr-sm-2(type="search", name='keyword',  placeholder="Brand Code",  aria-label="Search")
				//- 		button.btn.btn-outline-success.my-2.my-sm-0(type="submit") Search

				li.text-info
					span &nbsp;
					a.btn.btn-info(href="/vderDetail/"+crVder._id) hello: 
						if crVder.name && crVder.name.length > 0
							-a;
							span #{crVder.name}
						else
							span #{crVder.code}
						if crVder.role
							-a;
							span &nbsp;(#{Conf.sfRole[crVder.role]})
						if crVder.photo
							-a;
							img(src=ServerAddress+crVder.photo, width="30")
				span &nbsp;&nbsp;&nbsp;&nbsp;
				li: a.btn.btn-danger(href="/logout") Logout

.container
	.row
		div(style="height:80px")